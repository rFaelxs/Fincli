package com.rfaelxs.model;

/**
 * Define o tipo de uma transação financeira.
 */
public enum TipoTransacao {
    /** Representa uma receita — dinheiro que entra. */
    ENTRADA,
    /** Representa uma despesa — dinheiro que sai. */
    SAIDA
}
